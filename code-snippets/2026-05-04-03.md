# Safe dict get

**Date:** 2026-05-04

## Description

Nested lookup without raising KeyError.

## Code

```python
def deep_get(data, *keys, default=None):
    current = data
    for key in keys:
        if not isinstance(current, dict) or key not in current:
            return default
        current = current[key]
    return current
```
